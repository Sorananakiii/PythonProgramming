# tuble
# tuple is very similar to a list
# but one major difference is tuple is immutable. 
# We create a `tuple` using parentheses `()`.

t1 = ('a',)
type(t1)

t2 = ('a')
type(t2)

t = tuple('lupins')
print(t)

example_tuple = ('Dylan', 26, 167.6, True)
print(example_tuple)
